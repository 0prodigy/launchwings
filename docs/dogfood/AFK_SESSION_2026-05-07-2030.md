# AFK Session — 2026-05-07 20:30 UTC to 20:45 UTC

First AFK session per the protocol in `docs/operations/AFK_AGENT_PROMPT.md`. Founder said "keep building, don't stop until I interrupt; merge everything to the default branch."

## Shipped (cherry-picked onto `claude/solopreneur-launch-platform-PcSNn`)

- **DOGFOOD-LRS-12** — waitlist API silent-fail in the catch arm. The send-throw branch returned `ok:true,queued:false,warn:"send_failed"` with HTTP 200; frontend gates on `res.ok` so it presented as a success toast even when no email left the building. Now returns HTTP 502 with `{ok:false,message:...}`. Frontend (`components/waitlist-form.tsx:79`) handled `!res.ok` already, no client edit needed. Branch `fix/lrs-12-waitlist-silent-fail` (commit `7b2c894`), cherry-pick `55da645` on production. Ticket: `docs/tickets/dogfood-LRS-12-waitlist-silent-fail.md`.

- **DOGFOOD-LRS-08** — meta description trimmed 172 → 146 chars (`apps/web/app/layout.tsx`). Within Google's 160-char SERP budget, four load-bearing claims preserved (always-on, audit, multi-channel, paying customers). Branch `fix/lrs-08-meta-description-trim` (commit `bd06aaa`), cherry-pick `f946a20` on production.

- **WEB-001** — build-time link-availability check. Static-analysis Node script that, after `next build`, parses every prerendered HTML file under `.next/server/app`, extracts URLs from `og:image`, `twitter:image`, `link rel*=icon`, `manifest`, `apple-touch-icon`, `preload as=image`, and fails CI if any URL has no matching `apps/web/public/<path>` or Next 15 file-convention route. Wired as `pnpm check:assets`; GitHub Actions workflow `.github/workflows/web-001-link-check.yml` runs on PR + push to production + nightly cron. Smoke-tested both directions (passes current build with 2 URLs resolved; fails on injected bogus link rel=icon). Branch `feat/web-001-link-availability-check` (commit `56115ba`), cherry-pick `55617e1` on production. Ticket: `docs/tickets/web-001-build-time-link-availability.md`.

All three cherry-picks merged cleanly onto `3e5a30d`; production HEAD is now at the WEB-001 cherry-pick. Local `pnpm install --frozen-lockfile && pnpm type-check && pnpm build && pnpm check:assets` is green on the merged state. Pushed feature branches to origin individually for traceability; production branch pushed as well per founder instruction.

## Consulted the team on

None this session. Every decision lined up with explicit prior consensus:
- LRS-12 / LRS-08 / WEB-001 acceptance criteria already specified in `docs/tickets/` or `docs/architecture/SETUP-01-monorepo-design.md` §11. Tier 1 work, no consultation needed.
- SETUP-01a hoist (the queue's first item) discovered to require a Vercel dashboard change → escalated to founder rather than splitting consultation across `architect`+`cto`.

## Blocked on the founder

- **SETUP-01a workspace hoist (PR1).** Cannot proceed without a Vercel dashboard change that AFK Tier 3 forbids me from making. Specifically, per `docs/architecture/SETUP-01-monorepo-design.md` §3.5: change Vercel project's **Root Directory** to `apps/web` (already correct, please confirm) AND set **Install Command** to `pnpm install --frozen-lockfile` running at the **repo root** (not `apps/web`). Reason: pnpm workspaces use a single root `pnpm-lock.yaml`; the moment we add `pnpm-workspace.yaml` at the repo root, `apps/web/pnpm-lock.yaml` is superseded and removed. If Vercel's install command runs in `apps/web`, it will not find a lockfile and the production deploy breaks. 60-second dashboard change. Once it's made, kick off PR1 — design doc has the literal moves.

- **`launchwings.com` Resend domain verification (carry-over).** AFK prompt's snapshot says this still presents the silent-fail symptom; LRS-12's fix now surfaces the failure as HTTP 502 instead of a misleading success, but the underlying domain verification is still founder-only ops in the Resend dashboard. The waitlist will keep returning 502s until the domain is verified.

- **Vercel project settings audit (one-time).** Per the production-pin advice in `SETUP-01-monorepo-design.md` §10: confirm "Production Branch = `claude/solopreneur-launch-platform-PcSNn`" and (preferably) auto-deploy is gated. Currently every push to that branch deploys to prod, including this one — that is consistent with the snapshot but is the only reason the AFK prompt's "don't push to production" rule exists. If the founder wants AFK pushes to NOT auto-promote, this is the dashboard knob. Not blocking; flagging.

## What I did NOT touch and why

- **`SETUP-01b..07`** (api skeleton, db, auth, agents, etc.) — strictly downstream of SETUP-01a per the design's §12 sequencing. Skipping ahead is explicitly disallowed in the AFK queue.

- **`LRC-01` audit harness** — Phase 1 work, depends on SETUP-04 landing (`packages/agents` + Trigger.dev). Out of reach this session.

- **`DOGFOOD-LRS-06` favicon + brand mark** — needs a designed logo SVG; that's a founder design call. Without it, generating a placeholder favicon would be scope creep masquerading as "just shipping something." WEB-001's check correctly does NOT require a favicon to ship — it only validates URLs that are referenced in HTML, and the current `app/layout.tsx` doesn't reference any favicon URL.

- **`LRS-DNS-001`, `EMAIL-001`, `T&S-002`, `SEC-001`, `DEPLOY-001`** — Phase 2/3 platform tickets, way out of MVP scope right now. Listed in `HANDOFF_NEXT_PHASE.md` as carry-over.

- **Build-in-public posts** — no draft published this session; the AFK queue does not list one and posting requires founder action anyway. The pinned positioning thread is still queued at `docs/dogfood/posts/` (folder not yet created).

- **No new `docs/dogfood/learnings.md` entry.** Nothing surprised me this session that wasn't already in the file. The "SETUP-01a needs Vercel dashboard" insight is captured in this report, not as a learning, because it's a one-time architectural blocker not a recurring friction.

## Decision log (Tier 2 actions taken on team consensus)

None. All three shipped items were Tier 1.

## What I'd do next

In priority order, assuming the founder unblocks Vercel dashboard:

1. **SETUP-01a workspace hoist** — exact moves are in `docs/architecture/SETUP-01-monorepo-design.md` §3. Estimate: 1 hour of edits + 15 min validation. Validation gate: `pnpm install` + `pnpm dev` + Vercel preview URL responds 200.

2. **SETUP-01b api skeleton** — depends on 01a + a Fly.io account (Tier 3 founder).

3. **`DOGFOOD-LRS-06` favicon** — only blocked on the founder providing or approving a brand mark. Once unblocked, brand-mark + favicon + apple-touch-icon ship in one PR; WEB-001's check stays green automatically because the new URLs will resolve to public/ files.

If SETUP-01a stays blocked on the Vercel dashboard, the next-most-leveraged unblocked work is **`STACK.md` follow-up** (architect §9): a short doc pinning the 18 Stage 1 evaluator stack choices (Firecrawl / cheerio / PageSpeed Insights API) so each evaluator ticket inherits them. Pure docs, no infra dependency, ~1 hour.

## Push log

- `fix/lrs-12-waitlist-silent-fail` → origin (new branch)
- `fix/lrs-08-meta-description-trim` → origin (new branch)
- `feat/web-001-link-availability-check` → origin (new branch)
- `claude/solopreneur-launch-platform-PcSNn` → origin (3 cherry-picks: `55da645`, `f946a20`, `55617e1`)

Vercel will auto-deploy the production-branch push. Founder should watch the deploy log; if it goes red, the most likely cause is the new GitHub Actions workflow file confusing some integration, in which case revert the WEB-001 cherry-pick (`55617e1`) only — the LRS-12 and LRS-08 cherry-picks are safe and self-contained.
